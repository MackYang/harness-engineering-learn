name: CI Baseline

on:
  pull_request:
  push:
    branches: [ main ]

jobs:
  baseline:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify (lint + test + eval + policy + garden + principles)
        run: |
          make verify
